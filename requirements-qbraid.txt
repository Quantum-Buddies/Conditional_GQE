# qBraid-compatible dependencies for the Conditional-GQE 40-qubit scaling pipeline.
# Install in qBraid Lab with: pip install -r requirements-qbraid.txt
# For B200 NVFP4 support, additionally run:
#   pip install --no-build-isolation transformer_engine[pytorch]

# Core
pyyaml>=6.0
numpy>=2.0
scipy>=1.14
torch>=2.0
matplotlib>=3.9
tqdm>=4.66

# Quantum chemistry
openfermion>=1.7
openfermionpyscf>=0.5
pyscf>=2.13

# Qiskit (used for baselines and exact diagonalization)
qiskit>=2.0
qiskit-algorithms>=0.4
qiskit-nature>=0.7

# NVIDIA CUDA-Q (required for H-cGQE two-stage pipeline)
# Note: install via pip inside qBraid Lab's GPU instance.
cudaq>=0.10.0
cudaq-solvers[gqe]

# qBraid SDK (for QPU job submission and device listing)
qbraid>=0.9.0

# PDF generation for submission report
fpdf2>=2.8

# Testing
pytest>=8.0

# NVFP4 mixed precision (B200/Blackwell only)
# Uncomment after spinning up a B200 instance:
# transformer-engine[pytorch]>=1.0
